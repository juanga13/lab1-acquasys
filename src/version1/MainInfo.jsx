import React, {Component} from 'react';

export default class MainInfo extends Component {
  render() {
    return (
      <div>
        <h3>Main Info</h3>
        <p>Hi there this is main info, where all the new info is shown, like classes,
        methods of teaching, philosophy, etc...</p>
      </div>
    );
  };
}